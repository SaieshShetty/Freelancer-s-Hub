<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen flex items-center justify-center">
    <div class="bg-gray-800 text-white p-8 rounded-lg shadow-lg w-full max-w-lg">
        <h2 class="text-3xl font-semibold mb-6 text-center">Edit Your Profile</h2>
        
        <!-- Form Start -->
        <form action="/edit/<%= user._id %>" method="post" enctype="multipart/form-data">
            <!-- Profile Photo -->
            <div class="mb-4 text-center">
                <label class="block mb-2 text-gray-400">Profile Photo</label>
                <div class="relative w-24 h-24 mx-auto mb-4">
                    <img class="w-full h-full object-cover rounded-full" src="/imag/uploads/<%= user.profilePhoto %>" alt="Profile Photo">
                </div>
                <input type="file" name="profilePhoto" class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-indigo-600 file:text-white hover:file:bg-indigo-500">
            </div>

            <!-- Username -->
            <div class="mb-4">
                <label for="username" class="block text-gray-400 mb-2">Username</label>
                <input type="text" id="username" name="username" class="w-full px-4 py-2 bg-gray-700 text-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-gray-800" placeholder="Enter your username">
            </div>

            <!-- Email -->
            <div class="mb-4">
                <label for="email" class="block text-gray-400 mb-2">Email</label>
                <input type="email" id="email" name="email" class="w-full px-4 py-2 bg-gray-700 text-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-gray-800" placeholder="Enter your email">
            </div>

            <!-- Date of Birth -->
            <div class="mb-6">
                <label for="Age" class="block text-gray-400 mb-2">Age</label>
                <input type="number" id="Age" name="Age" class="w-full px-4 py-2 bg-gray-700 text-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-gray-800">
            </div>
            
            <!-- Phone Number -->
            <div class="mb-6">
                <label for="Phno" class="block text-gray-400 mb-2">Phone Number</label>
                <input type="number" id="Phno" name="Phno" class="w-full px-4 py-2 bg-gray-700 text-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:bg-gray-800">
            </div>

            <!-- Save Button -->
            <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-500 text-white font-semibold py-2 px-4 rounded-lg">Save Changes</button>
        </form>
        <!-- Form End -->

    </div>
</body>
</html>
